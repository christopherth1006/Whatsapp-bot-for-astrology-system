const User = require('./User.model');
const Location = require('./Location.model');
const AstrologyData = require('./AstrologyData.model');
const Playlist = require('./Playlist.model');

// Export all models
module.exports = {
  User,
  Location,
  AstrologyData,
  Playlist,
};
